export type ProductType = {
    _id: string
    image: string
    carbs: number
    fats: number
    proteins: number
    calories: number
    name: string
    createdAt: string
    updatedAt: string
}
